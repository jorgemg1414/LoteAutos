# ── Base de datos ─────────────────────────────────────────────────────────────
# Crea un cluster gratuito en https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://<usuario>:<contraseña>@cluster0.xxxxx.mongodb.net/loteautos?retryWrites=true&w=majority

# ── JWT ────────────────────────────────────────────────────────────────────────
# Cadena aleatoria larga (mínimo 32 caracteres)
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=cambia_esto_por_una_cadena_aleatoria_muy_larga

# ── Admin ─────────────────────────────────────────────────────────────────────
ADMIN_USERNAME=admin

# Opción A — desarrollo (texto plano, NO usar en producción)
ADMIN_PASSWORD=tu_contraseña_aqui

# Opción B — producción (hash bcrypt, recomendado)
# Genera el hash con: node -e "import('bcryptjs').then(({default:b})=>console.log(b.hashSync('tu_contraseña',10)))"
# ADMIN_PASSWORD_HASH=$2b$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Entorno ────────────────────────────────────────────────────────────────────
NODE_ENV=development
